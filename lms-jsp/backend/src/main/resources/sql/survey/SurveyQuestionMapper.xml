<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.happyjob.study.domain.survey.dao.SurveyQuestionDao">

	<!-- 특정 설문의 모든 문항 조회 -->
	<select id="listQuestionsBySurveyId" resultType="kr.happyjob.study.domain.survey.model.SurveyQuestionModel">
		SELECT
				question_id as questionId
			,	content
			,	type
			,	`order`
			,	survey_id as surveyId
		FROM survey_question
		WHERE survey_id = #{surveyId}
		ORDER BY `order` ASC
	</select>

	<!-- 설문 문항 단건 조회 -->
	<select id="getQuestionById" resultType="kr.happyjob.study.domain.survey.model.SurveyQuestionModel">
		SELECT
				question_id as questionId
			,	content
			,	type
			,	`order`
			,	survey_id as surveyId
		FROM survey_question
		WHERE question_id = #{questionId}
	</select>

	<!-- 설문 문항 저장 -->
	<insert id="insertQuestion" parameterType="java.util.Map">
		<selectKey resultType="long" keyProperty="questionId" order="BEFORE">
			SELECT IFNULL(MAX(question_id), 0) + 1 FROM survey_question
		</selectKey>
		INSERT
		INTO survey_question
			(
				question_id, content, type, `order`, survey_id
			)
		VALUES
			(
				#{questionId}
			,	#{content}
			,	#{type}
			,	#{order}
			,	#{surveyId}
			)
	</insert>

	<!-- 설문 문항 수정 -->
	<update id="updateQuestion">
		UPDATE survey_question
		SET
				content = #{content}
			,	type = #{type}
			,	`order` = #{order}
		WHERE question_id = #{questionId}
	</update>

	<!-- 설문 문항 삭제 -->
	<delete id="deleteQuestion">
		DELETE
		FROM survey_question
		WHERE question_id = #{questionId}
	</delete>

	<!-- 특정 설문의 모든 문항 삭제 -->
	<delete id="deleteQuestionsBySurveyId">
		DELETE
		FROM survey_question
		WHERE survey_id = #{surveyId}
	</delete>

</mapper>
